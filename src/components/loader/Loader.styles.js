import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  h4: {
    textAlign: "center"
  },
  container: {
    width: "100%",
    marginBottom: "10px"
  }
}))